/*
 * Приложение — оконное, а не консольное, в любой сборке.
 *
 * Подсистема выбирается на этапе сборки и записывается в заголовок файла:
 * консольному приложению Windows заводит консоль при рождении процесса, ещё до
 * первой строки нашего кода. Спрятать её потом можно, увидеть до этого — тоже.
 *
 * Заготовка Tauri ставит этот признак только для релиза, чтобы в разработке
 * оставался вывод. Но вывод от подсистемы не зависит: `tauri dev` запускает нас
 * с перенаправленным потоком и читает его сам, а `println!` пишет в него в
 * любом случае. Зато при запуске с ярлыка отладочная сборка выкидывала на экран
 * чёрное окно консоли — и ярлыки наборов упирались ровно в это.
 */
#![windows_subsystem = "windows"]

fn main() {
    quickrapidx_lib::run()
}
