/** Запись приложения — общая форма для стартового набора и сохранённых записей. */
export type AppEntry = {
	id: string;
	site: string;
	download: string;
	download_plus: string;
	programm_ico: string;
	programm_name: string;
	programm_description: string;
};
