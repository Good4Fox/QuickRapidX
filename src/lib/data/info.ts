/**
 * Метка стадии рядом с названием в титлбаре.
 *
 * Здесь только запасное значение: подпись берётся из словаря по ключу
 * `app_stage`, потому что по-японски это ベータ, а не латиница. Пусто здесь
 * оставить нельзя — метку рисуют и до того, как словарь поднимется.
 */
export const APP_TAG = 'Beta';

/** Подсказка на метке — показывается при наведении. */
export const APP_TAG_TOOLTIP = '0.5.0';
